{
  "server_messages": {
    "format": "[Serveur {id}] {message}",
    "patterns": [
      "*bruit vaisselle* Table {table}, ils prennent {order}",
      "*brouhaha* Tu peux noter pour la {table} ? {order}",
      "*passage client* La {table}, ils commandent {order}"
    ]
  },
  "kitchen_messages": {
    "format": "[Cuisine {id}] {message}",
    "patterns": [
      "*bruit casseroles* Ça sort dans {time} minutes !",
      "*four qui sonne* On est à {time} de retard",
      "*vapeur* Attends {time} minutes ok ?"
    ]
  }
}
